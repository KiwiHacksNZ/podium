# Playwright E2E Test Environment Variables
# Playwright's webServer boots the backend, which reads secrets from
# backend/.env (see the root .env.example). No extra config needed here.

# Optional: API URL (defaults to http://127.0.0.1:8000)
PUBLIC_API_URL=http://127.0.0.1:8000
